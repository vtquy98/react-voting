type Category {
  id: ID!
  name: String!
  description: String!
}

type Query {
  get_all_category: [Category!]!
}

type Mutation {
  create_category(name: String!, description: String!): Category!
}
